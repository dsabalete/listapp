{% extends '::base.html.twig' %}

{% block body -%}
    
    <div class="jumbotron">
        <h1>Lista de listas</h1>
    </div>

    <p class="listcreate">Nueva lista</p>
    
    <table class="table-striped">
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                    <div class="listedit" id="{{ entity.id }}">{{ entity.name }}</div>
                </td>
                <td>
                    <a href="{{ path('list_show', { 'id': entity.id }) }}" class="btn btn-link">detalle</a>
                    <a href="{{ path('list_items', { 'id': entity.id }) }}" class="btn btn-link">items</a>
                </td>
            </tr>
        {% endfor %}
            
        </tbody>
    </table>
    
   
    {% endblock %}
