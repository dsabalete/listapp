{% extends '::base.html.twig' %}

{% block body -%}
    
    <div class="jumbotron">
        <h1>Detalle</h1>
    </div>
    
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.task }}</td>
            </tr>
            <tr>
                <th>Fecha de creación</th>
                <td>{{ entity.created|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Completado</th>
                <td>{{ completado }}</td>
            </tr>            
        </tbody>   
    </table>

    <ul class="pager">
        <li><a href="{{ path('list') }}">Volver a la lista</a></li>
    </ul>
   
    {% endblock %}
